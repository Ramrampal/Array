#include<iostream>
#include<algorithm>
using namespace std;
 int main(){
      int n;
       cin>> n;
       int arr[n];
       int size = sizeof(arr)/sizeof(arr[0]);
        for( int i=0; i<size; i++){
            cin>> arr[i];
        }
    
      int min = *min_element( arr, arr+ size);
      int maxV = *max_element( arr, arr+size);
 
      
          cout<< min<<endl;
           cout<< maxV;
      
        return 0;
 }
